server:
  port: 8082

spring:
  application:
    name: account-creation-service

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    service-url:
      defaultZone : http://127.0.0.1:8080/eureka/